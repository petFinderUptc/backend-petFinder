/**
 * Enum de tamaños de mascota
 *
 * Clasificación estándar por peso.
 */

export enum PetSize {
  SMALL = 'small', // Pequeño (0-10kg)
  MEDIUM = 'medium', // Mediano (10-25kg)
  LARGE = 'large', // Grande (25kg+)
}
