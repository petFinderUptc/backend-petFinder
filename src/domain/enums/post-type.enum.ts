/**
 * Enum de tipos de publicación
 *
 * Define si la publicación es de una mascota perdida o encontrada.
 */

export enum PostType {
  LOST = 'lost', // Mascota perdida
  FOUND = 'found', // Mascota encontrada
}
