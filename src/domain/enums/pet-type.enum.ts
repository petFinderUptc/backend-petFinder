/**
 * Enum de tipos de mascota
 *
 * Define las especies de mascotas soportadas por el sistema.
 */

export enum PetType {
  DOG = 'dog',
  CAT = 'cat',
  BIRD = 'bird',
  RABBIT = 'rabbit',
  OTHER = 'other',
}
